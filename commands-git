Cloner un repository: git clone "nom du repository" içi git clone https://github.com/anthony-yrles/git-begins.git

Les 3 commandes de push sont :
- git add . (pour tous les fichiers) ou git add "nom-du-fichier"
- git commit -m "non explicite du commit"
- git push

Commande pour pull : "git pull".

Comment créer une nouvelle branche:
Il suffit d'utiliser la commande "git branch nom-de-la-branche" içi "git branch add-create-branch-command"

Afin de se déplacer sur la branche on utilisera "git checkout nom-de-la-branche" ici "git checkout add-create-branch-command"

Une branche créée depuis le terminal de commande ne sera possible a push que si on tape la commande "git push --set-upstream origin add-create-branch-command"

Petit plus qualité, après avoir push les modifications de la branche nous pourrions retourner sur le main grace à "git checkout main" et récupérer les modifications graçe a git merge add-create-branch-command
